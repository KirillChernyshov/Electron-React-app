// import {ipcRenderer} from 'electron'
// import {setPing} from '../slices/processDataSlice'
//
// export default function initPingsListeners(store: any) {
//   ipcRenderer.on('data-flow', (event, data) => {
//     store.dispatch(setPing(JSON.stringify(data)));
//   })
// }
